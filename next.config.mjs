/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
    domains: ['s.gravatar.com', 'cdn.auth0.com', 'images.ctfassets.net', 'lh3.googleusercontent.com'],
  },
};

export default nextConfig;
